# Copiez ce fichier en .env et remplissez les vraies valeurs (jamais commit .env lui-même).
# Utilisé par src/lib/waitlist.ts pour envoyer les inscriptions de la liste d'attente vers Notion.

# Settings → Connections → Developer connections → créez une intégration, copiez son "Internal Integration Secret".
NOTION_TOKEN=

# L'ID de la base Notion (dans son URL : notion.so/xxxxx?v=... → xxxxx est le NOTION_DB_ID).
# La base doit avoir une propriété "Email" (titre) et "Contacté" (case à cocher), et être
# partagée avec l'intégration (⋯ en haut à droite de la base → Connexions → ajouter l'intégration).
NOTION_DB_ID=

# Utilisé par src/lib/contact.ts pour envoyer le formulaire de contact par email (via Resend).
# Créez un compte gratuit sur resend.com, copiez la clé API depuis le dashboard.
RESEND_API_KEY=

# Sans domaine vérifié sur Resend, vous ne pouvez recevoir que sur l'adresse utilisée
# pour créer le compte Resend. Mettez donc la même adresse ici.
CONTACT_TO_EMAIL=
